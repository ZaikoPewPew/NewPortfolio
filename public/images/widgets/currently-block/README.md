# Currently Block — видео

Положите сюда превью для hover-блока в хедере:

- `0630.mov` — основной ролик (без звука, loop при hover)

Путь задан в `src/config/site.config.ts` → `employer.video`.

Если файла нет, блок показывает placeholder-фон без видео.
