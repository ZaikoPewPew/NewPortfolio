# Live case media

Сюда кладите ассеты боевых кейсов (`visibility: live`).

Путь в MDX: `/images/cases/live/...`

Папка в `.gitignore` (кроме этого README и `.gitkeep`) — файлы не попадут в git.
Локально / на деплое с `PUBLIC_CASES_SHOW_LIVE=true` положите ассеты сюда вручную.
