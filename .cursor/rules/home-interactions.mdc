---
description: Hover-фон и интеракции главной страницы
globs: src/features/home/**
alwaysApply: false
---

# Home Interactions

## Hover кейсов

- Единственный эффект — currently-block с медиа у курсора (`CaseHoverController` + `caseFocus.client.ts`); системный курсор не меняется
- Медиа карточки — `data-hover-video` / `data-hover-image` (frontmatter `hover.previewVideo` / `hover.previewImage`; приоритет: видео → картинка → `siteConfig.employer.video`)
- Каждый вход курсора на плитку — видео **с начала** (`CurrentlyBlockActivateOptions.restart`); employer hover без restart
- Класс `is-case-active` на `html` / `[data-home-page]` — увеличивает размер блока (`--currently-block-case-*`)
- Карточка получает `.is-active` (показ тегов)
- Никаких blur / wash / градиента фона / preview-панели / подъёма карточки
- Логика выхода, scale блока — `case-hover/README.md`

## Ограничения

- Hover-блок отключён на mobile и `prefers-reduced-motion`
- Фон — `PageBackground.astro` (сплошной `--color-bg-page`)

## Оркестрация

`HomeOrchestrator.client.ts` инициализирует hover, feedback, hotkeys на главной.
