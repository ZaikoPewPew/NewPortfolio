---
description: Layouts, страницы и View Transitions
globs: src/{layouts,pages}/**
alwaysApply: false
---

# Layouts & Pages

## Layouts

- `BaseLayout.astro` — HTML-оболочка, meta, View Transitions, `data-page` на body, `PageBackground` (hover-градиент кейсов)
- `CaseLayout.astro` — разметка детальной страницы кейса (hero slot + content)

## Pages

- `pages/index.astro` — главная: виджеты + кейсы + HomeOrchestrator
- `pages/cases/[slug].astro` — детальный кейс из Content Collection
- `pages/404.astro` — страница «не найдено» (базовая; стили в `features/not-found/`)

## Сетка

Размеры колонок, gutter и breakpoint — в `layout-grid.mdc`. Страницы не задают ширины напрямую.

## Правила

- Новые маршруты — только в `src/pages/`, не в components
- View Transitions — через `astro:transitions` в BaseLayout, стили в `features/transitions/`
- Страницы собирают layout + features; бизнес-логику hover/dock не класть в pages
- `page` prop BaseLayout: `home` | `case` | `not-found` | `default` — для page-scoped стилей
