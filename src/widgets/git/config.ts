import type { WidgetConfig } from "../_shared/types";

export const gitConfig: WidgetConfig = {
  id: "git",
  ariaLabel: "GitHub активность",
  layout: "wide",
  grid: { col: 2, row: 1 },
  status: "live",
};
