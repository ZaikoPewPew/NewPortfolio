import type { WidgetConfig } from "../_shared/types";

export const photoConfig: WidgetConfig = {
  id: "photo",
  ariaLabel: "Фото",
  layout: "square",
  grid: { col: 1, row: 1 },
  status: "mock",
};
