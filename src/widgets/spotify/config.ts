import type { WidgetConfig } from "../_shared/types";

export const spotifyConfig: WidgetConfig = {
  id: "spotify",
  ariaLabel: "Spotify — сейчас играет",
  layout: "square",
  grid: { col: 1, row: 1 },
  status: "mock",
};
