import type { WidgetConfig } from "../_shared/types";

export const bentoConfig: WidgetConfig = {
  id: "bento",
  ariaLabel: "Ссылки и каналы",
  layout: "square",
  grid: { col: 1, row: 2 },
  status: "mock",
};
